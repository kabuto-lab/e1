# Session-plan · Phase A — Schema Foundation (work4u → NAS)

| Field | Value |
|---|---|
| **Mode** | MANUAL |
| **Started** | 2026-05-26 10:22 |
| **Epic** | work4u-into-NAS migration · Phase A of K |
| **Source-of-truth doc** | `barbie/NON_PROJECT/MIGRATION_PLAN_work4u_into_NAS_2026-05-25.md` v1.1 §2, §8 Phase A |
| **Council adjustments** | Table 2 row Phase A (`governance/COUNCIL-COMPARISON.html`) — ADR-001 same session; isolation specs before Phase B; composite `(tenant_id, …)` indexes mandatory |
| **Activation Matrix row** | "New domain module / new Drizzle aggregate" — Tier-1 + Tier-2 + Test Pilot + Migrator |
| **Quorum** | Standard (Tier-1 + Tier-2) + Adversarial (TestPilot) + Evolutionary (Migrator) |
| **First-line status** | `[mode:MANUAL] phase:phase-A-schema-foundation epic:work4u-into-NAS spine:pending` |

---

## §0 · Assumed entering state (T1 read-before-trust, verified)

| Claim | Verified via | Result |
|---|---|---|
| SITE1 schema has 22 files in `packages/db/src/schema/` | `ls` | ✓ |
| `tenants.ts` lacks `site_type` column | `Read` | ✓ — needs ALTER |
| Drizzle journal at idx 3 (`0003_tenant_bootstrap`) | `cat _journal.json` | ✓ — Phase A adds idx 4 |
| 4-layer tenant isolation already wired (`tenant.guard.ts`, ALS context, `withTenant`, NOT NULL) | `Read tenant.guard.ts` | ✓ |
| `@SkipTenant()` decorator exists | `Read tenant.decorator.ts` | ✓ |
| work4u/packages/db/src/schema has 10 source-tables matching plan §2.1 | `ls` | ✓ |
| No `partner-salons.ts` or `wfy-*.ts` or `lead-applications.ts` in SITE1 schema yet | `ls` | ✓ — pristine |
| No `governance/adr/ADR-001*.md` exists before this session | (drafted in this session) | ✓ |

No drift detected vs MIGRATION_PLAN v1.1 §8 Phase A expectations.

---

## §1 · Declared scope (D-1 budget)

**New files (all new, no overwrites):**

| # | Path | Status | LOC budget |
|---|---|---|---|
| 1 | `SITE1/packages/db/src/schema/partner-salons.ts` | new | 80 |
| 2 | `SITE1/packages/db/src/schema/wfy-city-pages.ts` | new | 100 |
| 3 | `SITE1/packages/db/src/schema/wfy-opportunities.ts` | new | 60 |
| 4 | `SITE1/packages/db/src/schema/wfy-vacancies.ts` | new | 70 |
| 5 | `SITE1/packages/db/src/schema/wfy-advantages.ts` | new | 50 |
| 6 | `SITE1/packages/db/src/schema/lead-applications.ts` | new | 90 |
| 7 | `SITE1/packages/db/src/schema/index.ts` | **modified** — add 6 re-exports | +12 |
| 8 | `SITE1/packages/db/src/schema/tenants.ts` | **modified** — add `siteType` column | +14 |
| 9 | `SITE1/packages/db/drizzle/0004_*.sql` | new (generated by drizzle-kit) | n/a |
| 10 | `SITE1/packages/db/drizzle/meta/_journal.json` | **modified** — drizzle-kit appends entry | n/a |
| 11 | `SITE1/packages/db/drizzle/meta/0004_snapshot.json` | new | n/a |
| 12 | `SITE1/apps/api/scripts/check-tenant-coverage.ts` | new (ADR-001 IMPL-A) | 150 |
| 13 | `SITE1/apps/api/src/tenant-context/coverage.allow.json` | new (ADR-001 IMPL-C) | 30 |
| 14 | `SITE1/apps/api/test/fixtures/missing-tenant-guard.controller.ts` | new (ADR-001 IMPL-B) | 20 |
| 15 | `SITE1/apps/api/test/check-tenant-coverage.spec.ts` | new (ADR-001 IMPL-B) | 60 |
| 16 | `SITE1/apps/api/package.json` | modified — add `check:tenant-coverage` script + `lint` chain | +3 |
| 17 | `SITE1/apps/api/test/partner-salons-tenant-isolation.spec.ts` | new (Council req.) | 80 |
| 18 | `SITE1/apps/api/test/wfy-tables-tenant-isolation.spec.ts` | new (combines 4 wfy tables) | 120 |
| 19 | `SITE1/apps/api/test/lead-applications-tenant-isolation.spec.ts` | new (Council req.) | 80 |

**LOC budget (sum):** ~1100. D-1 threshold: > 30% overrun = scope split to next session.

**Spine-touch ledger:**

| File | Why spine | Authorization mode required |
|---|---|---|
| `schema/partner-salons.ts` (new) | `packages/db/src/schema/*.ts` glob is spine | explicit operator OK |
| `schema/wfy-city-pages.ts` (new) | same | explicit operator OK |
| `schema/wfy-opportunities.ts` (new) | same | explicit operator OK |
| `schema/wfy-vacancies.ts` (new) | same | explicit operator OK |
| `schema/wfy-advantages.ts` (new) | same | explicit operator OK |
| `schema/lead-applications.ts` (new) | same | explicit operator OK |
| `schema/index.ts` (modify) | same — adds re-exports for above | explicit operator OK |
| `schema/tenants.ts` (modify) | same — adds `siteType` | explicit operator OK |

All other files (scripts, tests, package.json, generated SQL, journal) are **non-spine** under ES `CLAUDE.md §M`:
- `drizzle/*.sql` — "только добавлять новые" → new migration files are non-spine
- `apps/api/scripts/`, `apps/api/test/` — non-spine
- `apps/api/package.json` — non-spine (only adding script entry)

---

## §2 · Council pre-pass (T2–T10 abridged for this MANIFEST)

### ORCHESTRATOR (T2)
- **Epic alignment:** matches `MIGRATION_PLAN §8 Phase A` verbatim. No blueprint cell to cite (NAS uses Epic engine, not 240-cell plan).
- **Dependency status:** Phase B depends on Phase A schema + isolation specs green. Phase D depends on Phase A + ADR-001 detector installed (so new controllers are gated). No prereqs missing.
- **Forward-inheritance map:** Phase B (work4u mig adapter) reads from these schemas; Phase D (admin CRUD) writes; Phase F (Data-blocks) reads.
- **Drift detectors:** D-1 budget declared above (1100 LOC). D-3 detector itself shipped this session (ADR-001 IMPL).
- **Verdict:** approve-with-conditions (operator OK on spine touches).

### HISTORIAN (T3)
- **Graph delta:** ADR-001 drafted today, status `Proposed`, ratify-by 2026-06-02. No graph contradictions (clean session — no prior ratified ADRs to compare with).
- **Verdict:** consistent.

### FORGEMASTER (T4)
- **Query budget:** new tables. Each tenant-scoped table gets composite index `(tenant_id, <natural-sort-col>)`. Drizzle-generated SQL must be reviewed for index usage before `db:migrate`.
- **Index plan:**
  - `partner_salons_tenant_ord_idx` on `(tenant_id, ord)`
  - `wfy_city_pages_tenant_slug_uniq` on `(tenant_id, slug)` + `wfy_city_pages_tenant_published_idx` on `(tenant_id, status)` if status added
  - `wfy_opportunities_tenant_ord_idx` on `(tenant_id, ord)`
  - `wfy_vacancies_tenant_ord_idx` on `(tenant_id, ord)`
  - `wfy_advantages_tenant_ord_idx` on `(tenant_id, ord)`
  - `lead_applications_tenant_created_idx` on `(tenant_id, created_at)`, `lead_applications_tenant_source_idx` on `(tenant_id, form_source)`
- **Validation boundary:** new tables get Zod schemas in services (Phase D). This session = schema only, no controllers.
- **Verdict:** approve.

### SENTINEL (T5)
- **Tenant isolation evidence (per new table):**
  | Table | tenant_id NOT NULL | FK CASCADE on tenants | Composite index | Isolation spec planned |
  |---|---|---|---|---|
  | `partner_salons` | ✓ | ✓ | ✓ | ✓ |
  | `wfy_city_pages` | ✓ | ✓ | ✓ | ✓ |
  | `wfy_opportunities` | ✓ | ✓ | ✓ | ✓ |
  | `wfy_vacancies` | ✓ | ✓ | ✓ | ✓ |
  | `wfy_advantages` | ✓ | ✓ | ✓ | ✓ |
  | `lead_applications` | ✓ | ✓ | ✓ | ✓ |
- **Failure modes named:**
  - **F-1 · `siteType` ALTER on tenants fails mid-flight.** Existing rows have `default 'generic-cms'` — Postgres ADD COLUMN with default and NOT NULL on small table (<10 rows in dev) is fast and atomic, but Drizzle generates it as `ALTER ... ADD COLUMN ... DEFAULT ... NOT NULL` which Postgres 11+ does in O(1) with no rewrite. Recovery: rollback via `ALTER TABLE tenants DROP COLUMN site_type;`.
  - **F-2 · `partner_salons` FK to `media.id` (logoMediaId) makes media table effectively tenant-cross-referenced.** Media already has `tenant_id`. Repo-layer must verify `media.tenant_id = partner_salons.tenant_id` on insert/update. Spec covers this.
- **Threat surfaces:** no new public endpoints in Phase A. Threat surface = unchanged.
- **Rollback path:** new tables = `DROP TABLE`. `siteType` column = `DROP COLUMN`. Drizzle `db:migrate:down` not auto-generated; rollback SQL hand-written and included in this session's commits as `0004_rollback.sql` (non-applied, kept for reference).
- **Observability hook:** none new (schema only).
- **Verdict:** approve-with-mitigations (F-2 mitigation spec required green before Phase B).

### SIMPLIFIER (T6)
- **Removable surfaces attempted:**
  - Could `wfy_advantages` and `wfy_opportunities` be a single `wfy_pinned_blocks` table with `type` discriminator? **Rejected** — schemas differ (advantages = 6 short bullets; opportunities = card with image + price + description). Combining now is speculative scaffolding (F-4).
  - Could `lead_applications` use `application_type` enum instead of `formSource` string? **Considered, kept string.** `formSource` is a code-controlled value (`wfy-interview`, `imp-booking`); string is more flexible across site types per §3.2. Migration to enum if values stabilise.
  - Could the four `wfy_*` tables be merged into one `wfy_blocks` with jsonb body? **Rejected** — destroys index usage and admin form generation; defeats §7 rule-of-three's premise that wfy is the *first* tenant of this shape.
- **Concrete reduction:** none accepted this session.
- **Verdict:** accept-as-is (after attempted reduction).

### ECONOMIST (T7)
- **Infra delta:** 6 new tables in same Postgres = negligible storage at launch (≤ 100 rows wfy total, ≤ 5 partner_salons). MinIO: no change (media reused via FK).
- **Per-tenant scaling:** `wfy_*` tables — currently 1 tenant (work-for-you). O(N) on tenants × O(rows/tenant). For work-for-you specifically: ~57 city_pages × 5 partner_salons × 3 vacancies × 6 advantages × 1 opportunities-block × N lead_applications. All bounded < 1000 rows per tenant.
- **`lead_applications` scaling:** unbounded with site usage. Index on `(tenant_id, created_at)` keeps queries O(log N) for admin pagination.
- **Maintenance cost:** new schemas = +6 files to keep current. No new alerts. No new runbook page.
- **Cheaper variant:** §7 rule-of-three already prevents premature generalisation; this is the cheapest correct option.
- **Verdict:** accept.

### MIGRATOR (T9)
- **Migration safety:** forward-only ✓. ADD COLUMN with default is non-destructive. CREATE TABLE is non-destructive.
- **Rollback test:** hand-written `0004_rollback.sql` shipped non-applied for reference (does NOT register in journal). Real rollback in dev = `db:reset` (acceptable on Phase 0/1).
- **WP-import fidelity:** N/A this phase (Phase L).
- **API shape impact:** no API surface changed this phase.
- **Verdict:** approve.

### TEST PILOT (T8)
- **Workload assumed:** none (schema only — no endpoints yet).
- **Verdict:** Council: TEST PILOT skipped — reason: schema-only session, no hot-path code introduced. Re-activates in Phase D (controllers landing).

### ADVERSARY (T8)
- **Triggers checked:** no new public endpoint; no upload; no input parsing; no WP-import. Adversary's full Phase A surface = the `lead_applications` table shape (it WILL be written to by Phase F ContactForm public endpoint). Pre-emptive concerns recorded in `governance/memory/adversary_init.md`:
  - jsonb `fields` and `attached_media_ids` must be size-capped on write (Phase F). Recommend `fields` ≤ 16 KB, `attached_media_ids` ≤ 20 ids — Adversary will gate Phase F session-plan.
- **Verdict (this phase):** Council: ADVERSARY skipped — reason: no public input surface introduced; pre-emptive concerns logged for Phase F.

### CHAOS (T8)
- **Triggers checked:** new Drizzle migration is in-scope. `db:migrate` runs without traffic in dev; in prod (VPS), `vps:after-pull` script runs migration before PM2 reload. Drill: what if `0004` partially applies?
  - PostgreSQL DDL is transactional in PG 11+ for ADD COLUMN, CREATE TABLE — atomic. Either fully applied or rolled back.
  - Drizzle journal updates AFTER successful migration — if PG crashed mid-statement, journal does not advance; re-run is idempotent.
- **Verdict:** approve.

### PRODUCTOR (T9)
- **Activation:** none — no admin UI in this phase. Productor activates Phase D.
- **Verdict:** Council: PRODUCTOR skipped — reason: no admin surface introduced this session.

### JUDGE (T10)
- **Conflict:** none — all entity verdicts harmonised.
- **Verdict:** N/A.

### T11 — Execute
**BLOCKED on operator OK for spine MANIFEST (§1).**

### T12 — Closing ritual
Will append to `SESSION_LOG.md` after T11 completes. Sections:
- Outcome per phase
- AI-Default decisions (n/a — MANUAL mode)
- Spine touches (8 files; mode: MANUAL operator OK)
- Commits (separate commit per logical unit: schemas, migration, ADR-001 IMPL, isolation specs)
- Recommendations for human review

### T13 — Anti-drift sweep
- D-1: scope at end vs declared 1100 LOC budget → record in SESSION_LOG.
- D-3: ADR-001 detector enabled this session → first run against current 18 controllers expected to populate `coverage.allow.json` with 5-8 legitimate skips.
- D-5: journal advances from idx 3 → idx 4 with 0004 entry → record.
- D-6: this session-plan ref in commit messages.
- D-7: no cross-module imports introduced (schema files only depend on `./tenants` and drizzle-orm).

---

## §3 · Operator decision gate

This session is **paused at T11** until operator resolves the spine MANIFEST:

**Option (A) — Approve spine MANIFEST in MANUAL mode:** confirm "OK на spine MANIFEST Phase A" → I write the 8 spine files one-by-one with brief verify between each.

**Option (B) — Upgrade to SEMIAUTO:** prefix next message with `SEMIAUTO:` → I execute all 8 spine files + isolation specs + ADR-001 IMPL without per-file stops, using this session-plan as the MANIFEST. Each AI-Default recorded in commit messages.

**Option (C) — Upgrade to AVTONOM:** prefix next message with `AVTONOM:` → I execute everything, log SKIP-pattern if any unexpected spine surfaces, write SESSION_LOG, never push. SEMIAUTO is recommended over AVTONOM here because schema files are foundational — per-MANIFEST review is light cost, large benefit.

**Option (D) — Scope down:** operator names a subset (e.g. "только partner_salons + isolation spec" first → next session covers wfy_*) — I redo MANIFEST and present again.

**Option (E) — Defer:** "не сейчас" → I commit ADR-001 + this session-plan as standalone artifacts (both non-spine) and the schema work waits.

---

## §4 · Why operator gate is unskippable

`barbie/ENTITY.md` Immutable I-13 + ES `CLAUDE.md §M` lock spine touches in MANUAL/SEMIAUTO to explicit operator OK. Bypassing this in MANUAL mode is F-12 (operator-bypass) and F-14 (spine touches without authorization). Council §F-12 and §F-14 are non-negotiable (Constitution §13 end-directive).

In AVTONOM mode, spine touches are SKIPped with SESSION_LOG entry — meaning Phase A schema would not actually land, only ADR-001 + this plan. So AVTONOM here is the *least* productive option without a separate spine-OK from operator.

---

**End of session-plan.**
Pending operator's decision per §3.

{
  "name": "@barbie-site1/db",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node ./run-migrate.mjs",
    "db:migrate:kit": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:check-state": "node ./check-state.mjs",
    "build": "tsc",
    "clean": "rimraf .turbo node_modules dist",
    "lint": "tsc --noEmit && node ./check-state.mjs",
    "check-types": "tsc --noEmit",
    "test": "node --test ./check-state.spec.mjs"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "drizzle-kit": "^0.28.0",
    "dotenv": "^16.4.7",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}

import { TenantModelsPage } from '@/components/tenant-site/TenantModelsPage';

export const metadata = { title: 'Анкеты — OUTCALL' };

export default function ModelsPage() {
  return <TenantModelsPage slug="outcall-massage" />;
}

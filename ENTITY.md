# ENTITY — Escort Platform: конституция совместной работы

**Назначение:** единая точка для человека и ИИ — как развивать продукт **без регрессий**, куда смотреть перед правками и как выкатывать на VPS. Между чатами «памяти» нет: опирайся на этот файл, **`CLAUDE.md`** (gstack и быстрый старт), код и `.env.example`.

**Репозиторий:** GitHub `kabuto-lab/e1`. **Локально (типично):** `C:\Users\a\Documents\_DEV\Tran\ES`. **VPS:** `~/e1`.

---

## 1. Стек и границы монорепо (факт, не выдумывать)

| Пакет | Технологии | Роль |
|--------|------------|------|
| `apps/api` | NestJS, Drizzle, JWT, Swagger | HTTP API, бизнес-логика, БД, MinIO |
| `apps/web` | Next.js 15 (App Router), React 19, Tailwind | UI, прокси `/api` в dev |
| `packages/db` | Drizzle ORM, PostgreSQL | Схемы, миграции |

**Инфраструктура:** PostgreSQL, Redis, MinIO (S3), Mailhog — см. `docker-compose.dev.yml`. **Не Prisma** — только Drizzle.

В репозитории есть посторонние деревья (`NON_PROJECT/`, `enso-develop/`, и т.д.) — **продукт** = `apps/*` + `packages/*` из workspaces.

---

## 2. Правила изменения кода (чтобы не ломать)

1. **Читать** затрагиваемые файлы целиком или достаточный контекст; **искать** вхождения по репо; пути брать из дерева проекта, не придумывать.
2. **Совместимость:** не менять поведение без явной просьбы; правки **точечные**; стиль как у соседнего кода (импорты, слои, паттерны Nest/Next).
3. **UI / визуал:** сначала **`DESIGN.md`**; при смене публичных контрактов/архитектуры сайта — при необходимости **`apps/web/public/platform-blueprint.html`**.
4. **Надёжность UX:** учитывать успех / ошибку / загрузку и сбои сети, **401 / 503** там, где уместно.
5. **Зависимости:** не добавлять пакеты без нужды.
6. **Документация:** не раздувать markdown без запроса; этот файл и `CLAUDE.md` — исключение как опорные.

**Формат работы ИИ:** по объёму задачи — для крупных изменений кратко: контекст → файлы → что сделано → как проверить; мелочи — коротко.

---

## 3. Источники правды (порядок чтения)

| Вопрос | Файл |
|--------|------|
| Типографика, цвета, сетка, motion | `DESIGN.md` |
| Потоки данных, модули API/Web/БД | `docs/ARCHITECTURE_CODEBASE.md`, `docs/CODEBASE_GUIDE.md` |
| Аудит, долги, приоритеты | `COMPREHENSIVE_AUDIT_AND_PLAN.md` |
| Роадмэп этапов 0–9 в UI | `apps/web/public/platform-blueprint.html` (вкладка «Роадмэп») |
| Пример переменных окружения | `.env.example` |
| Деплой Nginx/PM2 текстом | `docs/DEPLOY_SERVER.md` |

---

## 4. Фаза продукта и очередь работ (ориентир)

**Phase 1 (Core CMS) ~65%** — см. актуальную формулировку в `CLAUDE.md` / `COMPREHENSIVE_AUDIT_AND_PLAN.md`.

Ближайшие направления (не ломая текущее): видимость изображений, альбомы, fade slider, water shader, booking UI и платежи.

---

## 5. Локальная разработка (кратко)

- **Docker Desktop (Windows):** если `docker compose` не видит движок — запустить Docker Desktop (`C:\Program Files\Docker\Docker`).
- Поднять сервисы: `docker-compose -f docker-compose.dev.yml up -d`
- API: `cd apps/api && npx ts-node -r tsconfig-paths/register src/main.ts` → **:3000**, Swagger `/api/docs`
- Web: `cd apps/web && npm run dev` → **:3001**
- Тестовый вход: `test@test.com` / `password123` (см. README)

**Корневые скрипты:** `npm run dev:apps`, `npm run build`, `npm run db:bootstrap` (миграции + сиды моделей при пустой БД).

---

## 6. VPS (`~/e1`) — выкатка без «двух ошибок» (503 каталог / пароль БД)

### Одна причина двух симптомов

API не подключается к PostgreSQL (**часто 28P01**) → Nest отдаёт **503** с сообщением про пароль. Каталог `/models` и дашборд показывают одну и ту же беду с разным UI.

### Почему ломается после pull

1. **Пароль в Docker-томе** задаётся при **первом** создании volume; смена `POSTGRES_PASSWORD` в compose **не** обновляет существующий том → расхождение с `DATABASE_URL` в `~/e1/.env`.
2. **`pm2 restart escort-api`** сохраняет **старый** `process.env`. После `.env` нужен **`startOrReload`**: **`npm run pm2:reload-api`** (см. `ecosystem.config.cjs`).

### Регламент

| Когда | Что сделать |
|--------|-------------|
| Закончил правки локально | **commit + `git push`** — иначе VPS не увидит скрипты и фиксы |
| На VPS после `git pull` | Из `~/e1`: **`npm run vps:after-pull`** (`npm ci` → `build` → **`ensure:database`** → **`pm2:reload-api`**) |
| Новый `next build` на VPS | При процессе **`escort-web`** в PM2: **`pm2 reload escort-web`** (смена env для веба: **`--update-env`**) |
| Другой контейнер Postgres | В `.env`: **`POSTGRES_CONTAINER=имя`** |
| Postgres не в Docker | Вручную **`ALTER USER`** под пароль из `DATABASE_URL`, затем **`npm run pm2:reload-api`** |

### Команды-якоря

- `npm run vps:after-pull` — основной сценарий обновления на сервере
- `npm run ensure:database` — проверка + при Docker и 28P01 авто-`ALTER ROLE` под `.env`
- `npm run db:align-password` — только выравнивание пароля роли в контейнере
- `npm run verify:database` — проверка строки подключения; вызывается из `scripts/start-api-prod.mjs` перед стартом API

### Правило для ИИ в диалоге по SSH

Выдавать **строго по одной shell-команде** за сообщение (если пользователь не просит иначе); следующую — после результата. Типовая цепочка по смыслу: `git pull` → при необходимости `npm run vps:after-pull` (не список из пяти команд одним блоком).

---

## 7. Gstack (Claude Code)

Проект использует **gstack** — виртуальную команду; полный список навыков (`/browse`, `/ship`, …) и команда починки — в **`CLAUDE.md`**. Веб: только `/browse` из gstack, не подменять другими браузерными MCP без необходимости.

---

## 8. Тон коллаборации человек ↔ ИИ

Кратко: прямой фидбек, конкретика, итерации — норма; критика кода не равно личности; праздновать рабочий результат. Развёрнутые «истории» и цитаты не обязательны для работы — достаточно разделов 1–7 этого файла.

---

## 9. История документа

- Изначально — заметки о совместной работе и экспериментах (в т.ч. water ripple).
- **2026-04-06** — объединение с инженерными правилами из `CLAUDE.md`, VPS-регламент, ориентация на безопасные изменения кода.
- **2026-04-06+** — `CLAUDE.md` содержит gstack и краткий quick start; детальная «конституция» — здесь.

*Обновляй этот файл при смене критичных процессов (деплой, стек, источники правды).*
